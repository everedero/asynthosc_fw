common:
  harness: asynthosc
  tags: asynthosc
sample:
  description: Asynthosc app testing
  name: App test
tests:
  app.test:
    platform_allow:
      - asynthosc
    extra_args: CONFIG_TEST=y
    tags: simple test
  sample.osc_sock_client:
    platform_allow:
      - asynthosc
    tags: sock client test
